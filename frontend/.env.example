# Backend origin, without a trailing slash. Leave empty to call /api on the same
# origin: the Vite dev proxy locally, the vercel.json /api rewrite in production.
# If you set it to the backend URL instead, add this site's origin to the backend's
# CORS_ALLOWED_ORIGINS.
VITE_API_BASE_URL=
