[pytest]
DJANGO_SETTINGS_MODULE = config.settings
pythonpath = .
testpaths = trips
addopts = -m "not live" -q
markers =
    live: hits real upstream services (OSRM, Photon); run with: pytest -m live
filterwarnings =
    ignore::DeprecationWarning:hypothesis.*
